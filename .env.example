# Port for the markup viewer + WebSocket server (default: 14271)
# Walks forward up to +5 if the port is in use.
MARKUP_PORT=14271

# Fallback port variable (used if MARKUP_PORT is not set)
# PORT=14271
